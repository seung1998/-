# 지급보고서 작성 규칙 (외주비/재료비 등)

사용자가 엑셀 지급 데이터를 주고 "보고서 만들어줘" 라고 하면, 아래 형식을 기본값으로 사용한다.
예시 파일: `templates/지급보고서_템플릿_예시.html` (2026년 9월 외주비/재료비 지급 보고 — 구조·CSS 그대로 재사용하고 데이터만 교체할 것)

## 산출물
- **HTML 파일 1개** + **PDF 파일 1개**를 항상 같이 준다. 아티팩트로도 게시해서 링크를 준다.
- HTML은 `<title>`+`<style>`+본문만 있는 조각(fragment)이며 `<html>/<body>` 없음 — Artifact 발행 시 자동으로 감싸짐. 그대로 파일로 보내도 대부분 브라우저에서 열림.
- PDF는 Playwright(Chromium)로 렌더링: `page.emulate_media(media='print')` 후 `page.pdf(format='A4', landscape=True, print_background=True, margin=0)`. 컬럼이 많아 세로(A4)로는 잘리면 landscape 사용.

## 색감
- **항상 밝은(라이트) 고정 색상**. 다크모드 대응 코드 넣지 말 것 — `prefers-color-scheme` 미디어쿼리 없이 `:root` 라이트 팔레트만 사용, `body{color-scheme:light}` 명시. (사용자가 이 문서를 복붙/공유했을 때 보는 사람 시스템이 다크모드여도 항상 밝게 보여야 함.)
- 팔레트: 배경 `#eef1f5`, 카드 `#ffffff`, 텍스트 `#182233`, 강조(네이비) `#17395c`, 경고/빨강 `#a3261e`+연한배경 `#fbeceb`, 재료비/외주비 구성비 막대색 `--mat:#17395c`(재료비) / `--out:#3f8f78`(외주비).
- 폰트: Noto Serif KR(제목만) + Noto Sans KR(본문/표), Google Fonts.

## 레이아웃 구성 (위에서부터)
1. 제목(h1) + 부제
2. 헤드라인 콜아웃 박스 (좌측 굵은 accent 보더, 총 지급액 크게 강조)
3. 3분할 요약 카드 (예: 1차 지급액 / 2차 지급액 / 확인 필요 건수·금액— 경고성 카드는 빨간 톤)
4. 지급일자별 섹션(01, 02, ...): 각 섹션 상단에 **재료비 vs 외주비 구성비 막대바(composition bar)** — 외주비/외주비(T)/출장비는 전부 "외주비"로 합산, 각주로 명시
5. 매입현황 표 (지급일자별로 표 분리, 지급일자별 소계/매입처 소계는 넣지 않음 — 요청 없으면 생략)

## 표 컬럼 (매입현황 기준)
매입처 | 구분 | 투입정보 | 프로젝트명 | 계약금액 | 당월지급액 | 지급률 | VAT | 실지급액 | 잔액
(누적지급액은 요청으로 제외함 — 다시 필요하다면 계약금액 다음에 추가)

## 표 스타일 규칙 (중요 — 실수했던 부분)
- **정렬은 반드시 class 기반으로.** `nth-child(n)` 같은 위치 기반 선택자 쓰지 말 것. 매입처가 여러 줄로 묶여 rowspan 처리된 경우, rowspan 이후 행들은 앞의 매입처 `<td>`가 아예 없어서 DOM 상 컬럼 위치가 한 칸씩 밀린다 (실제로 이 버그로 계약금액이 절반은 왼쪽 정렬되는 사고가 있었음). `.vendor`, `.proj` 는 `text-align:left`, `.tag`/`.person`(구분/투입정보)은 `text-align:center`를 클래스에 직접 지정하고, 숫자 컬럼은 기본값 `text-align:right`에 맡긴다.
- 같은 매입처가 여러 프로젝트(여러 행)를 가지면 `rowspan`으로 매입처명 병합, 새 매입처 시작 행에는 `grp-start` 클래스로 위쪽에 굵은 구분선 추가 (매입처 경계를 명확히 구분하기 위함).
- 여러 표(지급일자별)에 걸쳐 컬럼 폭이 페이지마다 달라 보이지 않도록, **모든 표에 동일한 `<colgroup>` 퍼센트 폭 + `table-layout:fixed`** 적용 (표마다 auto 레이아웃이 달라지는 것 방지).
- **컬럼 폭/간격을 손으로 감으로 조정하지 말 것.** 사용자가 "이 컬럼이랑 저 컬럼 사이 간격이 안 맞는다"고 하면, 퍼센트를 추측해서 고치면 한쪽을 줄이다 다른 쪽이 벌어지는 문제가 반복된다. 대신: `table-layout:auto`로 잠시 풀고(`col`의 인라인 width도 제거) 브라우저가 계산한 실제 컬럼 폭을 Playwright로 측정한 뒤(`th.bounding_box()`, 표 전체 width 대비 비율 계산), 그 값을 그대로 `<colgroup>` 퍼센트로 되돌려 `table-layout:fixed`를 다시 적용한다. 컬럼마다 실제 내용에 맞는 폭이 나오므로 간격이 자연스럽게 고르게 맞는다.
  - **문서 자체 폭을 줄이고 싶다(컬럼/여백을 타이트하게)는 요청**은: `table{width:100%}`를 잠시 `width:auto`로 바꿔야 진짜 "내용에 필요한 최소 폭"이 측정된다 (width:100%인 채로는 auto 레이아웃이어도 컨테이너 폭에 맞춰 늘어나므로 참고용 비율만 알 수 있고 실제 축소된 폭은 안 나온다). 이렇게 구한 각 컬럼의 최소 폭 합계에 안전 버퍼(각 nowrap 숫자/텍스트 컬럼당 +5~7px 정도)를 더해 최종 표 폭을 정하고, `.sheet`의 `max-width`를 (표 폭 + 좌우 padding)으로 줄인다. 표 폭을 줄인 뒤에는 `overflow-x`/`scrollWidth vs clientWidth` 체크로 개행되면 안 되는 컬럼(매입처/구분/당월지급액/지급률/실지급액 등 `white-space:nowrap`)에 실제 줄바꿈·잘림이 없는지 코드로 확인한다.
- **"헤더 위치는 그대로 두고 값만 헤더 중심에 맞춰달라" 같은 요청**은 text-align을 맞추는 게 아니라 좌표를 직접 재서 맞춘다: 헤더 `<th>`에 `Range.selectNodeContents()+getClientRects()`로 실제 텍스트(글자) bounding box를 구해 그 중심 x좌표를 계산하고, 데이터 셀은 그대로 `text-align:center`를 쓰되 `padding-left`(또는 right)를 조정해 그 중심이 헤더 텍스트 중심과 일치하도록 만든다. 다시 측정해서 오차가 1px 미만인지 확인하고 넘어갈 것 — 눈대중으로 "이 정도면 됐겠지" 하고 넘기지 않는다. 지급률(당월포함)·실지급액(VAT포함) 두 컬럼 모두 이 방식(`.pct`, `.real` 클래스)으로 각자의 헤더 중심에 맞춰져 있다 — 지급률은 사용자가 "살짝 오른쪽으로"라고 명시적으로 요청해서 완전한 중심에서 +5.89px만큼 의도적으로 어긋나 있음(이 오프셋은 유지할 것), 실지급액은 완전한 중심 일치(오차 <0.15px).
  - **컬럼 폭(퍼센트)이 바뀌면 이 padding 오프셋도 반드시 다시 계산해야 한다** — 절대 px 값이라 컬럼 폭이 달라지면 어긋난다. 재계산법: 새 폭에서 오프셋 없이(plain `text-align:center`) 측정한 값의 중심과 헤더 중심의 차이를 재고, `diff(P) = diff(0) + 0.5*(P-0)`(패딩 1px당 중심이 0.5px 이동하는 선형관계)를 이용해 원하는 diff(지급률은 +5.89px 유지, 실지급액은 0)가 나오는 새 padding-left를 역산한다.
- **화면(HTML)과 인쇄(PDF)의 문서 폭이 다르면 픽셀 단위로 맞춘 정렬이 PDF에서 어긋난다.** `@media print`에서 `.sheet{max-width:none}`으로 A4 전체 폭을 쓰면 화면에서 측정·보정한 padding 값이 PDF에서는 틀어진다. 반드시 `@media print`의 `.sheet`도 화면과 **동일한 `max-width`+동일한 좌우 padding**(`margin:0 auto`로 페이지 중앙 배치)을 쓰도록 하고, 정렬을 검증할 때는 화면 뷰포트뿐 아니라 실제 PDF 레이아웃 폭(A4 landscape ≈ 1123px 뷰포트에서 `page.emulate_media('print')`로 측정)에서도 diff를 확인한다.
- **문서 전체 폭(`.sheet` max-width)은 860px, 좌우 padding은 화면/인쇄 동일하게 32px, `table.dense` 셀 padding은 `6px 4px`**가 최신 확정값 (2026-09 기준, 사용자가 참고 파일 폭 860px를 보고 확정). 표 폭은 860 - 32*2 = 796px. 다음에 이 폭보다 훨씬 긴 매입처명/프로젝트명이 들어오면 위 "최소 폭 측정" 방법으로 오버플로 여부부터 재확인할 것.
- 최신 완성 예시(가장 단순화된 형태, 헤더 6개: 매입처·구분·프로젝트명·당월지급액·지급률(당월포함)·실지급액(VAT포함), 요약카드 1개, 헤드라인 없음, 문서 폭 860px): `templates/per-date-example/report_1cha.html` (단일 매입처/1행), `report_2cha.html` (매입처당 동일 프로젝트 합산 + rowspan 다중행 예시) — 사용자가 다음에 "이 형식대로" 라고 하면 여기서부터 시작할 것.
- 잔액(또는 금액)이 0인 셀은 `0` 대신 `-` (하이픈)으로 표시.
- 매출 회수가 안 된 것으로 표시된(원본 엑셀에서 노란색 음영) 항목은 행 배경만 옅은 빨강으로 표시하고 프로젝트명 뒤에 `*` 표시 + 표 하단 각주 한 줄로 설명. 뱃지/pill 남발하지 말 것(전에 별도 "확인필요" 뱃지+색 텍스트까지 넣었다가 "보기 힘들다"는 피드백 받음 — 최소한으로).
- **사용자가 이 HTML을 다른 곳(워드/노션/메일/카톡 등)에 복붙해서 쓴다.** 표의 음영 행(매출 미확인 등)은 클래스+스타일시트만으로 배경색을 주면 복붙 시 자주 빠진다 — 각 `<tr>`과 그 안의 모든 `<td>`에 `style="background:#hex"` 인라인을 클래스와 중복으로 넣을 것.
  - **빈 `<div>`/`<span>`에 `background`만 줘서 만든 색칠 박스(막대·점 등)는 인라인 `style="background:#hex"`를 추가해도 복붙하면 색이 빠질 수 있다** (실제로 재료비/외주비 구성비 막대에서 겪음 — 인라인을 넣었는데도 사용자가 복붙해보니 색이 안 들어갔다고 재확인됨). Word/Outlook/Notion 등 붙여넣기 대상 앱들이 "내용 없는 요소의 background"를 걸러내는 경우가 많기 때문으로 보인다. **근본 해결은 구조를 바꾸는 것**:
    - 색칠된 막대/바(구성비 막대 등)는 flex `<div>`가 아니라 `<table><tr><td>`로 만들고, 각 `<td>`에 **CSS `background-color`(★`background` 축약형 아님) + 레거시 `bgcolor="#hex"` 속성을 함께**, 그리고 `<td>` 안에 `&nbsp;` 같은 **실제 콘텐츠**를 넣는다(내용이 없는 셀은 배경이 씹히는 경우가 있음). `table.dense`처럼 `border-collapse:collapse;table-layout:fixed`로 폭을 퍼센트 지정.
    - 점(범례 dot 등) 같은 작은 색상 표시는 배경색 박스 대신 **실제 색칠된 텍스트 글자**(`●` 등 유니코드 문자에 `style="color:#hex"`)로 대체한다 — `color`는 어떤 붙여넣기 대상에서도 거의 항상 유지되는 가장 기본적인 속성이라 background보다 훨씬 안전하다.
    - 실제로 잘 복붙되는지 의심될 때는 눈으로 보지만 말고, Playwright로 `document.execCommand('copy')` 후 `navigator.clipboard.read()`로 `text/html` 타입을 읽어 실제 클립보드에 담기는 HTML(특히 `background-color`/`bgcolor`/텍스트 `color`가 인라인으로 남아있는지)을 코드로 확인할 것.
  - 새 색상 요소를 추가할 때마다 "빈 요소의 background로 만들고 있진 않은가, table+bgcolor나 텍스트 color로 바꿀 수 있는가"를 체크리스트로 확인할 것.
- 합계(tfoot) 행은 위아래 굵은 선 + 옅은 배경으로 박스처럼 강조.

## 매입처당 건수가 많을 때 (합산)
사용자가 "같은 매입처에 같은 프로젝트가 여러 건 있으면 합산해서 하나로 만들어줘"라고 하면:
- 매입처+프로젝트명이 동일한 행들만 합산 (다른 프로젝트는 그대로 별도 행 유지).
- 당월지급액·VAT·실지급액은 단순 합산. 지급률(당월포함)은 임의 평균이 아니라 `(Σ기존누적지급액 + Σ당월지급액) / Σ계약금액 * 100`로 가중 재계산 — 반드시 Python 등으로 코드 계산하고, 암산/어림값 쓰지 말 것 (과거 세션에서 암산으로 낸 값이 틀렸던 적 있음).
- 구분이 서로 다르면(예: 외주비 + 출장비) `"외주비·출장비"`처럼 가운뎃점으로 합쳐서 표시.
- 표 하단 각주에 합산 규칙 한 줄 추가.
- 합산 후 합계(tfoot)가 원본 총계와 정확히 일치하는지 코드로 재검증.

## 지급일이 여러 개일 때
기본은 하나의 문서에 지급일자별 섹션(01, 02, ...)을 나눠 넣는 것. 하지만 사용자가 "지급일 각각 따로 작성해줘" 라고 하면 **지급일마다 완전히 별도의 파일(HTML+PDF 각각)** 로 쪼갠다. 이 경우:
- 제목/헤드라인/부제를 그 지급일 하나에 맞게 다시 쓴다 ("1차/2차" 구분, 지급일자 명시).
- 섹션이 문서당 하나뿐이므로 `<span class="no">01</span>` 같은 번호 배지는 뺀다 (번호는 실제 순서가 있을 때만 의미 있음).
- 요약 카드 3개는 그 지급일에 실제로 있는 내용으로 채운다 — 매출회수 미확인 건이 없는 회차라면 억지로 "미확인 0건" 카드를 넣지 말고 "최대 지급처"처럼 그 회차에 실제 의미 있는 카드로 대체.
- CSS(`<style>` 블록)는 그대로 복사해서 재사용 — 새로 디자인하지 않는다.
- 예시: `templates/per-date-example/report_1cha.html`, `report_2cha.html`

## 작업 시 항상 지킬 것
- 사용자의 원본 엑셀 파일(수식/서식)은 절대 건드리지 않는다 — 보고서는 별도 산출물로 새로 만든다.
- 숫자는 전부 원본 데이터 그대로 사용, 임의로 계산해서 지어내지 않는다.
- 매번 결과를 스크린샷/PDF로 직접 열어서 확인 후 전달 (특히 페이지 분할·정렬은 육안 확인 없이 넘기지 말 것).
