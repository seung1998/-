# 지급보고서 작성 규칙 (외주비/재료비 등)

사용자가 엑셀 지급 데이터를 주고 "보고서 만들어줘" 라고 하면, 아래 형식을 기본값으로 사용한다.
예시 파일: `templates/지급보고서_템플릿_예시.html` (2026년 9월 외주비/재료비 지급 보고 — 구조·CSS 그대로 재사용하고 데이터만 교체할 것)

## 산출물
- **HTML 파일 1개** + **PDF 파일 1개**를 항상 같이 준다. 아티팩트로도 게시해서 링크를 준다.
- HTML은 `<title>`+`<style>`+본문만 있는 조각(fragment)이며 `<html>/<body>` 없음 — Artifact 발행 시 자동으로 감싸짐. 그대로 파일로 보내도 대부분 브라우저에서 열림.
- PDF는 Playwright(Chromium)로 렌더링: `page.emulate_media(media='print')` 후 `page.pdf(format='A4', landscape=True, print_background=True, margin=0)`. 컬럼이 많아 세로(A4)로는 잘리면 landscape 사용.

## 색감
- **항상 밝은(라이트) 고정 색상**. 다크모드 대응 코드 넣지 말 것 — `prefers-color-scheme` 미디어쿼리 없이 `:root` 라이트 팔레트만 사용, `body{color-scheme:light}` 명시. (사용자가 이 문서를 복붙/공유했을 때 보는 사람 시스템이 다크모드여도 항상 밝게 보여야 함.)
- 팔레트: 배경 `#eef1f5`, 카드 `#ffffff`, 텍스트 `#182233`, 강조(네이비) `#17395c`, 경고/빨강 `#a3261e`+연한배경 `#fbeceb`, 재료비/외주비 구성비 막대색 `--mat:#17395c`(재료비) / `--out:#3f8f78`(외주비).
- 폰트: Noto Serif KR(제목만) + Noto Sans KR(본문/표), Google Fonts.

## 레이아웃 구성 (위에서부터)
1. 제목(h1) + 부제
2. 헤드라인 콜아웃 박스 (좌측 굵은 accent 보더, 총 지급액 크게 강조)
3. 3분할 요약 카드 (예: 1차 지급액 / 2차 지급액 / 확인 필요 건수·금액— 경고성 카드는 빨간 톤)
4. 지급일자별 섹션(01, 02, ...): 각 섹션 상단에 **재료비 vs 외주비 구성비 막대바(composition bar)** — 외주비/외주비(T)/출장비는 전부 "외주비"로 합산, 각주로 명시
5. 매입현황 표 (지급일자별로 표 분리, 지급일자별 소계/매입처 소계는 넣지 않음 — 요청 없으면 생략)

## 표 컬럼 (매입현황 기준)
매입처 | 구분 | 투입정보 | 프로젝트명 | 계약금액 | 당월지급액 | 지급률 | VAT | 실지급액 | 잔액
(누적지급액은 요청으로 제외함 — 다시 필요하다면 계약금액 다음에 추가)

## 표 스타일 규칙 (중요 — 실수했던 부분)
- **정렬은 반드시 class 기반으로.** `nth-child(n)` 같은 위치 기반 선택자 쓰지 말 것. 매입처가 여러 줄로 묶여 rowspan 처리된 경우, rowspan 이후 행들은 앞의 매입처 `<td>`가 아예 없어서 DOM 상 컬럼 위치가 한 칸씩 밀린다 (실제로 이 버그로 계약금액이 절반은 왼쪽 정렬되는 사고가 있었음). `.vendor`, `.proj` 는 `text-align:left`, `.tag`/`.person`(구분/투입정보)은 `text-align:center`를 클래스에 직접 지정하고, 숫자 컬럼은 기본값 `text-align:right`에 맡긴다.
- 같은 매입처가 여러 프로젝트(여러 행)를 가지면 `rowspan`으로 매입처명 병합, 새 매입처 시작 행에는 `grp-start` 클래스로 위쪽에 굵은 구분선 추가 (매입처 경계를 명확히 구분하기 위함).
- 여러 표(지급일자별)에 걸쳐 컬럼 폭이 페이지마다 달라 보이지 않도록, **모든 표에 동일한 `<colgroup>` 퍼센트 폭 + `table-layout:fixed`** 적용 (표마다 auto 레이아웃이 달라지는 것 방지).
- 잔액(또는 금액)이 0인 셀은 `0` 대신 `-` (하이픈)으로 표시.
- 매출 회수가 안 된 것으로 표시된(원본 엑셀에서 노란색 음영) 항목은 행 배경만 옅은 빨강(`.flag`)으로 표시하고 프로젝트명 뒤에 `*` 표시 + 표 하단 각주 한 줄로 설명. 뱃지/pill 남발하지 말 것(전에 별도 "확인필요" 뱃지+색 텍스트까지 넣었다가 "보기 힘들다"는 피드백 받음 — 최소한으로).
- 합계(tfoot) 행은 위아래 굵은 선 + 옅은 배경으로 박스처럼 강조.

## 작업 시 항상 지킬 것
- 사용자의 원본 엑셀 파일(수식/서식)은 절대 건드리지 않는다 — 보고서는 별도 산출물로 새로 만든다.
- 숫자는 전부 원본 데이터 그대로 사용, 임의로 계산해서 지어내지 않는다.
- 매번 결과를 스크린샷/PDF로 직접 열어서 확인 후 전달 (특히 페이지 분할·정렬은 육안 확인 없이 넘기지 말 것).
