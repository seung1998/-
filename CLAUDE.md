# 작업 메모

## 보고서(예: 개인경비 지급 보고, 법인카드 사용내역 보고 등) 요청 시
- 결과물은 **PDF + HTML 두 가지 형식**을 함께 만들어서 전달할 것.
  - PDF: 열람/출력/공유용 (레이아웃이 고정되어 어디서 봐도 동일)
  - HTML: 드래그로 선택해 다른 문서(Word, 메일 등)에 복사-붙여넣기 할 때 표/서식이 그대로 유지되도록 제공
- HTML은 외부 폰트(Google Fonts 등)에 의존하지 않고 시스템 폰트(맑은 고딕 등)만 사용 — 오프라인에서 열어도 정렬이 깨지지 않도록 할 것.
- 표 정렬 규칙: 텍스트(라벨) 열은 왼쪽 정렬, 숫자 열은 오른쪽 정렬이 기본. 사용자가 특정 열(예: "주요 사용 내역")의 정렬을 별도로 요청하면 그 열만 반영.

### 최종 승인된 템플릿 (반드시 이 양식을 그대로 재사용할 것)
- 위치: `report-templates/personal-expense-report-template.html`
- 새 보고서 요청이 오면 이 파일을 베이스로 복사해서 **내용(숫자·부서명·날짜 등)만 교체**하고, CSS/레이아웃/정렬/폭은 임의로 바꾸지 말 것. 사용자가 형식 변경을 명시적으로 요청할 때만 수정.
- 확정된 스펙:
  - 본문 폭: `.page { max-width: 860px; width: 100%; margin: 0 auto; }` (유동폭, 780px 대비 약 10% 넓힘)
  - 색상: 배경 강조 박스 `#eef5f5` + 좌측 보더 `#175e63`, 증가 `#b3261e`(빨강)/감소 `#1a56a8`(파랑), 진한 텍스트 `#232833`
  - 표 정렬: 라벨(첫 열)만 왼쪽, 숫자 열은 오른쪽 정렬. 단, 섹션 01 "주요 사용 내역" 열은 헤더+내용 모두 **가운데 정렬**(사용자 요청으로 예외 처리됨).
  - 섹션 02(항목별 지급 현황) 표는 `<colgroup>`으로 열 폭 고정: 항목 16% / 8월·7월·전월대비·전년동월 각 21%로 균등 분배(간격 요청 반영).
  - 합계(총계) 행: 위/아래 모두 굵은 보더(`border-top`/`border-bottom: ...#3c4350`)로 구분선 표시.
  - PDF는 이 HTML을 Playwright(Chromium)로 렌더링 후 `page.pdf()`로 생성 — HTML의 `document.body.scrollWidth/scrollHeight`를 그대로 PDF 페이지 크기로 사용해 한 페이지에 꽉 차게 만들 것(잘리거나 여러 페이지로 쪼개지지 않도록).

### 법인카드 사용내역 보고 — 개인경비 보고서와는 별도 템플릿
- 법인카드 사용내역 보고는 개인경비 지급 보고와 요구사항(섹션 구성·정렬·헤더 등)이 다를 수 있으므로, 위 개인경비 템플릿 스펙을 그대로 적용하지 말고 **별도로 관리**할 것.
- 위치: `report-templates/corporate-card-report-template.html`
- 새 법인카드 보고서 요청이 오면 이 파일을 베이스로 복사해서 **내용(숫자·부서명·날짜 등)만 교체**하고, CSS/레이아웃/정렬/폭은 임의로 바꾸지 말 것. 사용자가 형식 변경을 명시적으로 요청할 때만 수정.
- 확정된 스펙(개인경비 템플릿과 다른 부분 위주):
  - 제목 아래 별도 subtitle 줄 없음 (개인경비 템플릿의 "구분 | 작성일" 줄과 달리, 작성일만 한 줄로 표기).
  - 헤드라인 박스 2번째 줄은 "사업부 금액 + 정부과제 금액"만 표기. "전월분 결제취소 차감" 같은 문구는 해당 데이터가 있을 때만 추가하고, 근거 데이터 없이 임의로 넣지 말 것.
  - 집행(지급) 예정일은 매번 실제 지급 일정에 맞춰 확인/반영 — 특정 요일 패턴을 임의로 가정하지 말 것.
  - 섹션 구성: 01 부문별 사용 현황 / 02 계정별 사용 현황 / 03 예산 대비 집행 현황 (개인경비 보고서의 "특이사항" 같은 서술형 섹션 없음).
  - 섹션 01 표: 헤더(th) 전부 **가운데 정렬**(사용자 요청 반영, 개인경비 템플릿과 다름). 본문(td)은 기본 규칙대로 라벨 좌/숫자 우 정렬, "주요 사용 내역"만 좌측 정렬. 총계(합계) 행의 "주요 사용 내역" 칸은 비워둘 것(문구 넣지 않음).
  - 섹션 01 표 하단에 "* 정부과제 사용액 …은 별도 관리 항목으로 상기 표에서 제외" 각주 표기, 정부과제는 부문별 표에서는 제외하되 섹션 02(계정별 표)에는 포함.
  - 섹션 02 표는 `<colgroup>`으로 열 폭 고정: 계정과목 16% / 8월·7월·전월대비·전년동월 각 21%로 균등 분배.
  - 섹션 03(예산 대비 집행 현황) 표는 6개 열(부문/연간예산/당월실적/누계/사용률/잔여예산)로 `<colgroup>` 고정 폭 사용, 대표이사·대외협력은 한 행으로 합산 표기.
  - 합계(총계) 행: 위/아래 모두 굵은 보더로 구분선 표시(개인경비 템플릿과 동일).
  - PDF 생성 방식(Playwright, scrollWidth/scrollHeight 기준 1페이지)은 개인경비 템플릿과 동일하게 적용.
