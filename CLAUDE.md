# 지급보고서 작성 규칙 (외주비/재료비 등)

사용자가 엑셀 지급 데이터를 주고 "보고서 만들어줘" 라고 하면, 아래 형식을 기본값으로 사용한다.
최신 확정 예시: `templates/per-date-example/report_1cha.html` (2026-09 1차, 아래 "최신 확정 레이아웃" 참고). 구조·CSS 그대로 재사용하고 데이터만 교체할 것.
(구버전 전체 템플릿 `templates/지급보고서_템플릿_예시.html` 은 참고용으로만 남겨둠 — 제목/헤드라인/3분할 카드/구성비 막대가 있던 초기 버전으로, 지금은 더 단순해졌다.)

## 산출물
- **HTML 파일 1개** + **PDF 파일 1개**를 항상 같이 준다. 아티팩트로도 게시해서 링크를 준다.
- HTML은 `<title>`+`<style>`+본문만 있는 조각(fragment)이며 `<html>/<body>` 없음 — Artifact 발행 시 자동으로 감싸짐. 그대로 파일로 보내도 대부분 브라우저에서 열림.
- PDF는 Playwright(Chromium)로 렌더링: `page.emulate_media(media='print')` 후 `page.pdf(format='A4', landscape=True, print_background=True, margin=0)`. 컬럼이 많아 세로(A4)로는 잘리면 landscape 사용.
- **이 보고서는 대개 기안(사내 결재 문서) 본문에 붙여넣는 첨부용 콘텐츠다.** 그래서 문서 자체의 큰 제목(h1)은 넣지 않는다 — 기안 양식이 이미 제목/기안자/결재란을 갖고 있어서 중복이면 어색해 보인다는 피드백을 받았다. 대신 헤드라인 문장(아래) 하나로 시작한다. 사용자가 "기안 상신 문구 넣어달라"거나 "첨부서류 확인해달라는 멘트 넣어달라" 하면 헤드라인 위/표 아래에 작은 글씨(14px 안팎, 헤드라인보다 튀지 않게)로 한 줄씩 추가 — 요청 없으면 넣지 않는다.

## 색감
- **항상 밝은(라이트) 고정 색상**. 다크모드 대응 코드 넣지 말 것 — `prefers-color-scheme` 미디어쿼리 없이 `:root` 라이트 팔레트만 사용, `body{color-scheme:light}` 명시. (사용자가 이 문서를 복붙/공유했을 때 보는 사람 시스템이 다크모드여도 항상 밝게 보여야 함.)
- 팔레트: 배경 `#eef1f5`, 카드 `#ffffff`, 텍스트 `#182233`, 강조(네이비) `#17395c`, 경고/빨강 `#a3261e`+연한배경 `#fbeceb`.
- 폰트: Noto Serif KR(옛 제목용, 지금은 미사용) + Noto Sans KR(본문/표 전체), Google Fonts.

## 최신 확정 레이아웃 (위에서부터) — 2026-09 기준
1. **(선택) 상단 안내 문구** — 기안 상신용 한 줄. 요청 있을 때만.
2. **헤드라인 콜아웃 박스** (`.headline`) — 좌측 굵은 accent 보더 + 옅은 배경, 문장형 한 줄: "8월 외주비·재료비 **903,386,146원**을 9월 10일(목) 지급 예정입니다." 금액만 크고 굵게(`<b>` 안에 `.headline-lead b`).
3. **매입현황 표** — 표 바로 위, `<thead>` 첫 행에 colspan 처리된 "(단위: 원)" 행 (별도 div 아님 — 이유는 아래 "복붙 시 위치 어긋남" 항목 참고).
4. **(선택) 하단 안내 문구** — "자세한 내역은 첨부서류를 확인해 주시기 바랍니다." 같은 한 줄. 요청 있을 때만, `.footnote` 클래스 재사용.

**과거에 있었다가 최종적으로 뺀 것들** (다시 기본값으로 넣지 말 것 — 사용자가 명시적으로 다시 요청할 때만):
- 문서 h1 제목 + 부제 (기안 첨부라서 중복)
- 3분할 요약 카드/표 (예: 누적지급액·당월지급액·잔액) — 사용자가 참고 이미지 보고 한 번 넣었다가, 결국 "그 표 부분 그냥 삭제해줘"로 최종 제거함
- 재료비/외주비 구성비 막대(composition bar) — "비율이 의미가 없어서" 삭제 요청받음
- 지급일자별 섹션 번호 배지(`01`, `02`) — 문서당 표가 하나뿐이면 의미 없음

## 표 컬럼 (2026-09 1차/2차 확정 기준)
매입처 | 구분 | 프로젝트명 | 매출률 | 지급률(당월포함) | 실지급액(VAT포함)
(당월지급액은 매출률로 교체됨, VAT·계약금액·잔액 등은 요청 시에만 추가 — 표 컬럼은 매번 사용자가 요청한 그대로 따를 것, 위 목록은 기본 출발점일 뿐)
- **매출률**과 **지급률(당월포함)**은 원본 엑셀의 서로 다른 열이다 — "매출현황 → 매출" 이 매출률, "매출현황 → 회수" 가 매출회수율(다른 개념). 사용자가 "매출률"이라고 하면 반드시 "매출" 열 값을 써야 하고, 헷갈리면 어느 열을 썼는지 먼저 밝히고 확인받을 것 — 실제로 회수 열을 잘못 매핑했다가 정정받은 적 있음.

## 헤더 정렬
- 표 헤더(`thead th`)는 기본 **가운데 정렬**. 다만 첫 컬럼(매입처)은 데이터가 좌측 정렬(`.vendor`)이라 헤더도 그쪽으로 살짝 당겨달라는 요청을 받아 `padding-right`로 미세 조정한 사례 있음(`.th-vendor`).
- **실지급액(VAT포함)** 헤더+값은 우측 정렬 (`.th-real`, `.real{text-align:right}`) — 표 위 "(단위: 원)" 문구가 우측 정렬이라 그 위치와 맞추기 위함. 이 컬럼만 예외적으로 오른쪽 정렬임에 주의.

## 선(border) 색/굵기 — 사용자가 세밀하게 지정한 부분, 임의로 바꾸지 말 것
- 표 헤더 바로 아래 밑줄: **완전 검정(#000000), 2.5px** — 강조.
- 매입처별 행 사이 구분선(`tbody tr.grp-start td`의 border-top): **얇은 회색(#c9d0dc), 2px** — 검정으로 바꾸지 말 것, 이전에 실수로 바꿨다가 되돌린 적 있음.
- 합계(tfoot) 위/아래 선: **완전 검정(#000000), 2.5px**.
- "(단위: 원)" 위쪽 구분선: 검정 계열, 표와 한 몸(아래 항목 참고). 아래쪽엔 선을 넣지 않음(표 헤더 밑줄이 그 역할을 겸함 — "선을 하나로 줄이자" 요청으로 정리됨).
- 이런 세부 선 스타일 요청이 오면 "어디부터 어디까지"를 표(헤더 위/헤더 아래/행 사이/합계 위아래)로 명확히 구분해서 하나씩 바꾸고, 바뀐 범위를 스크린샷으로 반드시 확인할 것 — 애매하게 "그 아래" 라고만 하면 어디를 가리키는지 되짚어 확인.

## 표 스타일 규칙 (중요 — 실수했던 부분)
- **정렬은 반드시 class 기반으로.** `nth-child(n)` 같은 위치 기반 선택자 쓰지 말 것. 매입처가 여러 줄로 묶여 rowspan 처리된 경우, rowspan 이후 행들은 앞의 매입처 `<td>`가 아예 없어서 DOM 상 컬럼 위치가 한 칸씩 밀린다 (실제로 이 버그로 계약금액이 절반은 왼쪽 정렬되는 사고가 있었음). `.vendor`, `.proj` 는 `text-align:left`, `.tag`(구분)은 `text-align:center`를 클래스에 직접 지정하고, 숫자 컬럼은 기본값 `text-align:right`에 맡긴다(단 실지급액은 위 예외 참고).
- 같은 매입처가 여러 프로젝트(여러 행)를 가지면 `rowspan`으로 매입처명 병합, 새 매입처 시작 행에는 `grp-start` 클래스로 위쪽에 구분선 추가.
- **태그 전용 선택자(`tbody td`, `thead th` 등, 클래스 없이)는 한 페이지에 표가 여러 개 있으면 서로 새어 들어갈 수 있다 (실제로 겪은 버그).** 브라우저는 `<table>` 안에 `<tbody>` 없이 바로 `<tr>`을 쓰면 **자동으로 암묵적 `<tbody>`를 삽입**한다. 그래서 별도 표(예: 3분할 요약을 표로 만든 것)에 `<tr><td>`만 써도, 데이터 표를 겨냥해 만든 `tbody td{...}` 규칙이 그 표에도 그대로 적용돼 정렬·패딩이 엉뚱하게 바뀐다. 데이터 표 전용 규칙은 항상 `table.dense th, table.dense td` 처럼 **클래스로 스코프**할 것 — 태그만 쓰는 규칙(`tbody td`, `thead th`)은 절대 추가하지 말 것.
- 여러 표(지급일자별)에 걸쳐 컬럼 폭이 페이지마다 달라 보이지 않도록, **모든 표에 동일한 `<colgroup>` 퍼센트 폭 + `table-layout:fixed`** 적용 (표마다 auto 레이아웃이 달라지는 것 방지).
- **컬럼 폭/간격을 손으로 감으로 조정하지 말 것.** 사용자가 "이 컬럼이랑 저 컬럼 사이 간격이 안 맞는다"고 하면, 퍼센트를 추측해서 고치면 한쪽을 줄이다 다른 쪽이 벌어지는 문제가 반복된다. 대신: `table-layout:auto`로 잠시 풀고(`col`의 인라인 width도 제거) 브라우저가 계산한 실제 컬럼 폭을 Playwright로 측정한 뒤(`th.bounding_box()`, 표 전체 width 대비 비율 계산), 그 값을 그대로 `<colgroup>` 퍼센트로 되돌려 `table-layout:fixed`를 다시 적용한다. 컬럼마다 실제 내용에 맞는 폭이 나오므로 간격이 자연스럽게 고르게 맞는다.
  - **문서 자체 폭을 줄이고 싶다(컬럼/여백을 타이트하게)는 요청**은: `table{width:100%}`를 잠시 `width:auto`로 바꿔야 진짜 "내용에 필요한 최소 폭"이 측정된다 (width:100%인 채로는 auto 레이아웃이어도 컨테이너 폭에 맞춰 늘어나므로 참고용 비율만 알 수 있고 실제 축소된 폭은 안 나온다). 이렇게 구한 각 컬럼의 최소 폭 합계에 안전 버퍼(각 nowrap 숫자/텍스트 컬럼당 +5~7px 정도)를 더해 최종 표 폭을 정하고, `.sheet`의 `max-width`를 (표 폭 + 좌우 padding)으로 줄인다. 표 폭을 줄인 뒤에는 `overflow-x`/`scrollWidth vs clientWidth` 체크로 개행되면 안 되는 컬럼에 실제 줄바꿈·잘림이 없는지 코드로 확인한다.
  - **폰트 크기를 키운 뒤에는 반드시 이 측정을 다시 해야 한다** — 글자가 커지면 같은 컬럼 폭이라도 줄바꿈/overflow가 새로 생길 수 있다(실제로 겪음: 매입처명이 2줄로 밀리고 구분 컬럼 값이 잘림). 폰트 사이즈를 바꿀 때마다 전체 컬럼폭 재측정 → 재배분 → overflow 재확인의 사이클을 거칠 것.
- **"헤더 위치는 그대로 두고 값만 헤더 중심에 맞춰달라" 같은 요청**은 text-align을 맞추는 게 아니라 좌표를 직접 재서 맞춘다: 헤더 `<th>`에 `Range.selectNodeContents()+getClientRects()`로 실제 텍스트(글자) bounding box를 구해 그 중심 x좌표를 계산하고, 데이터 셀은 그대로 `text-align:center`를 쓰되 `padding-left`(또는 right)를 조정해 그 중심이 헤더 텍스트 중심과 일치하도록 만든다. 다시 측정해서 오차가 1px 미만인지 확인하고 넘어갈 것.
  - **헤더가 2줄로 줄바꿈되면 `getClientRects()[0]`은 첫 줄만 잡는다.** 전체 줄바꿈 블록의 합집합(모든 rect의 min-left/max-right)을 구해야 정확한 중심이 나온다는 점에 주의 — 다만 헤더가 길게 줄바꿈되고 컬럼이 좁으면 "완벽한 중심"에 맞추려는 padding 값이 실제 데이터 내용보다 더 큰 여백을 요구해서 overflow가 날 수 있다. 이럴 땐 헤더의 **첫 줄(주 단어)** 을 기준으로 맞추거나, 아예 컬럼 폭을 넉넉히 넓혀서 여유를 확보할 것 — 억지로 맞추려다 숫자가 잘리는 것보다 낫다.
  - **컬럼 폭(퍼센트)이 바뀌면 이 padding 오프셋도 반드시 다시 계산해야 한다** — 절대 px 값이라 컬럼 폭이 달라지면 어긋난다. 재계산법: 새 폭에서 오프셋 없이(plain `text-align:center`) 측정한 값의 중심과 헤더 중심의 차이를 재고, `diff(P) = diff(0) + 0.5*(P-0)`(패딩 1px당 중심이 0.5px 이동하는 선형관계)를 이용해 원하는 diff가 나오는 새 padding-left를 역산한다.
- **화면(HTML)과 인쇄(PDF)의 문서 폭이 다르면 픽셀 단위로 맞춘 정렬이 PDF에서 어긋난다.** `@media print`에서 `.sheet{max-width:none}`으로 A4 전체 폭을 쓰면 화면에서 측정·보정한 padding 값이 PDF에서는 틀어진다. 반드시 `@media print`의 `.sheet`도 화면과 **동일한 `max-width`+동일한 좌우 padding**(`margin:0 auto`로 페이지 중앙 배치)을 쓰도록 하고, 정렬을 검증할 때는 화면 뷰포트뿐 아니라 실제 PDF 레이아웃 폭(A4 landscape ≈ 1123px 뷰포트에서 `page.emulate_media('print')`로 측정)에서도 diff를 확인한다.
- **문서 전체 폭(`.sheet` max-width)은 860px, 좌우 padding은 화면/인쇄 동일하게 32px**가 확정값 (사용자가 참고 파일 폭 860px를 보고 확정, 여러 차례 재확인 후 유지). 표 폭은 860 - 32*2 = 796px. 새 매입처명/프로젝트명이 훨씬 길면 위 "최소 폭 측정" 방법으로 오버플로 여부부터 재확인할 것.
- 잔액(또는 금액)이 0인 셀은 `0` 대신 `-` (하이픈)으로 표시.
- 매출 회수가 안 된 것으로 표시된(원본 엑셀에서 노란색 음영) 항목은 행 배경만 옅은 빨강으로 표시하고 프로젝트명 뒤에 `*` 표시 + 표 하단 각주 한 줄로 설명. 뱃지/pill 남발하지 말 것.
- 합계(tfoot) 행은 위아래 굵은 검정 선 + 옅은 배경으로 박스처럼 강조.

## 복붙(Word/HWP/Notion 등) 안전성 — 반복해서 겪은 문제들
이 보고서는 화면/PDF로만 보는 게 아니라 **다른 문서(특히 한글 기안 양식)에 복붙**해서 쓴다. 여기서 여러 번 문제가 생겼고, 원칙은 "화면에서 예쁘게 보이는 것"과 "복붙해도 안 깨지는 것"은 별개라는 것이다 — 반드시 복붙까지 검증할 것.

1. **색이 빠지는 문제**: 표의 음영 행은 클래스+스타일시트만으로 배경색을 주면 복붙 시 자주 빠진다 — 각 `<tr>`과 그 안의 모든 `<td>`에 `style="background:#hex"` 인라인을 클래스와 중복으로 넣을 것.
2. **빈 요소 배경도 빠질 수 있다**: 빈 `<div>`/`<span>`에 `background`만 줘서 만든 색칠 박스(막대·점 등)는 인라인을 추가해도 복붙하면 색이 빠질 수 있다(Word 등이 "내용 없는 요소의 background"를 걸러내는 것으로 보임). 근본 해결:
   - 색칠된 막대는 `<table><tr><td>`로 만들고, 각 `<td>`에 `background-color`(★`background` 축약형 아님) + 레거시 `bgcolor="#hex"` 속성을 함께, `<td>` 안에 `&nbsp;` 같은 실제 콘텐츠를 넣는다.
   - 점 같은 작은 색상 표시는 배경색 박스 대신 실제 색칠된 텍스트 글자(`●` 등 유니코드 문자에 `style="color:#hex"`)로 대체 — `color`가 background보다 훨씬 안전.
3. **같은 폭인 줄 알았던 두 요소가 복붙하면 어긋나는 문제** (표와 그 위/아래의 안내문구 한 줄 등): 화면에서는 둘 다 "컨테이너 100% 폭"이라 우연히 맞아 보이지만, Word 같은 붙여넣기 대상은 표의 컬럼 폭을 자기 방식대로 재계산하는 반면 옆의 `<div>`는 그대로 두기 때문에, 둘의 오른쪽 끝이 실제로는 서로 다른 계산에서 나온 "우연의 일치"였을 뿐이라 어긋난다. **해결: 위치를 맞춰야 하는 문구는 별도 div로 두지 말고, 그 표(또는 표로 변환한 요소) 안의 `colspan` 행으로 만들 것.** ("(단위: 원)" 문구를 표의 `<thead>` 첫 행 `<td colspan="6">`으로 옮긴 사례, "(부가세 제외 금액)" 문구를 3분할 요약을 flex→표로 바꾸면서 그 표의 두 번째 행으로 옮긴 사례 참고.)
4. **표로 바꿀 때 암묵적 `<tbody>` 버그** — 위 "표 스타일 규칙" 항목 참고. 새 표를 추가할 때마다 기존 태그 전용 규칙이 새어 들어오는지 반드시 확인.
5. **숫자/라벨에 `white-space:nowrap` 필수** — 붙여넣기 대상이 컬럼을 재계산해 더 좁게 만들면 긴 숫자가 중간에서 줄바꿈되며 "깨진" 것처럼 보인다. 큰 금액을 표시하는 `.stat-value` 류 요소에는 항상 nowrap을 걸어둘 것.
6. **검증 방법**: 눈으로 보지만 말고, Playwright로 `document.execCommand('copy')` 후 `navigator.clipboard.read()`로 `text/html`을 읽어 실제 클립보드에 담기는 HTML(인라인 색상, 표 구조, colgroup 폭)을 코드로 확인할 것. 다만 이 방법은 Chrome 자체의 복사 직렬화만 검증한다 — Word/한글 자체의 붙여넣기 재해석까지는 여기서 재현이 안 되므로, 사용자가 "복붙하니 깨진다"고 다시 말하면 **어느 프로그램에 붙였는지, 어떻게 깨졌는지(스크린샷)** 를 먼저 물어보고 고칠 것 — 이미 여러 번 구조를 바꿔봤는데도 계속 깨진다고 하면 추측성 수정을 반복하지 말고 반드시 확인부터 할 것.

## 매입처당 건수가 많을 때 (합산)
사용자가 "같은 매입처에 같은 프로젝트가 여러 건 있으면 합산해서 하나로 만들어줘"라고 하면:
- 매입처+프로젝트명이 동일한 행들만 합산 (다른 프로젝트는 그대로 별도 행 유지).
- 당월지급액·VAT·실지급액은 단순 합산. 지급률(당월포함)은 임의 평균이 아니라 `(Σ기존누적지급액 + Σ당월지급액) / Σ계약금액 * 100`로 가중 재계산 — 반드시 Python 등으로 코드 계산하고, 암산/어림값 쓰지 말 것 (과거 세션에서 암산으로 낸 값이 틀렸던 적 있음).
- 구분이 서로 다르면(예: 외주비 + 출장비) `"외주비·출장비"`처럼 가운뎃점으로 합쳐서 표시.
- 표 하단 각주에 합산 규칙 한 줄 추가.
- 합산 후 합계(tfoot)가 원본 총계와 정확히 일치하는지 코드로 재검증.

## 지급일이 여러 개일 때
지급일마다 완전히 별도의 파일(HTML+PDF 각각)로 쪼갠다 (사용자가 "지급일 각각 따로 작성해줘"라고 요청). 이 경우:
- 헤드라인 문장을 그 지급일 하나에 맞게 다시 쓴다 ("1차/2차" 구분, 지급일자·금액 명시).
- CSS(`<style>` 블록)는 그대로 복사해서 재사용 — 새로 디자인하지 않는다.
- 한 파일에 적용한 서식 변경은 기본적으로 **다른 회차 파일에는 자동으로 적용하지 않는다** — 사용자가 "이것도 다른 파일에 적용해줘"라고 명시적으로 요청할 때만 옮긴다 (한 파일만 계속 다듬다가 회차 간 서식이 크게 벌어질 수 있음, 최종 확정 후에는 반드시 다른 회차에도 동기화할지 물어볼 것).
- 예시: `templates/per-date-example/report_1cha.html`

## 작업 시 항상 지킬 것
- 사용자의 원본 엑셀 파일(수식/서식)은 절대 건드리지 않는다 — 보고서는 별도 산출물로 새로 만든다.
- 숫자는 전부 원본 데이터 그대로 사용, 임의로 계산해서 지어내지 않는다. 원본의 어느 열을 썼는지 헷갈리면 먼저 밝히고 확인받는다.
- 매번 결과를 스크린샷/PDF로 직접 열어서 확인 후 전달 (특히 페이지 분할·정렬·overflow는 육안+코드 확인 없이 넘기지 말 것).
- 사용자가 "최종으로 해줘" 라고 확정하면, 그 상태를 `templates/per-date-example/`에 동기화하고 이 문서(CLAUDE.md)도 최신 상태로 갱신할 것.
